<hc:PopupWindow x:Class="NewLaserProject.Views.AddToDbView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NewLaserProject.Views"
        mc:Ignorable="d"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        SizeToContent="WidthAndHeight"
        MinWidth="300"
        ShowCancel="True"
        
        >
    <Grid>
        <StackPanel>
            <hc:PropertyGrid Name="PropGrid" SelectedObject="{Binding}"
                         />
            <hc:ElementGroup Margin="{Binding ElementName=PropGrid, Path=Padding}" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="OkButton" Click="OkButton_Click"  Content="Добавить" Style="{StaticResource ButtonSuccess.Small}"/>
                
                <Button IsCancel="True" hc:IconElement.Geometry="{StaticResource DeleteGeometry}" Style="{StaticResource ButtonDanger.Small}"/>
            </hc:ElementGroup>
        </StackPanel>
    </Grid>
</hc:PopupWindow>
