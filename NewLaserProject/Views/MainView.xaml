<Window x:Class="NewLaserProject.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NewLaserProject.Views"
        mc:Ignorable="d"
        xmlns:uc="clr-namespace:MachineControlsLibrary.Controls;assembly=MachineControlsLibrary"
        Title="Лазер" Height="450" Width="800" WindowState="Maximized">
    <Grid>
        <Grid.Resources>
             <ResourceDictionary>
                <ResourceDictionary.MergedDictionaries>
                    <ResourceDictionary Source="pack://application:,,,/MachineControlsLibrary;component/Controls/Dictionary.xaml"/>
                </ResourceDictionary.MergedDictionaries>                
            </ResourceDictionary>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Column="1" Grid.RowSpan="2">
            <Border BorderBrush="CadetBlue" BorderThickness="1" DockPanel.Dock="Top" HorizontalAlignment="Left">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <TextBlock Text="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}, Path=DataContext.FileName}" Width="100" Margin="2" Background="LightYellow"/>
                    <Button Command="{Binding OpenFileCommand}" Content="open file" Style="{StaticResource CommandButton}"/>
                    <Button Command="{Binding OpenLayersProcessingCommand}" Content="Layers processing" Style="{StaticResource CommandButton}"/>
                </StackPanel>
            </Border>
            <uc:GraphWindow 
                           LayGeoms="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}, Path=DataContext.LayGeoms}"                                            
                           SpecMargin="0.2" 
                           SpecSizeX="60000"
                           SpecSizeY="48000"
                           AutoProportion="True"
                           StrokeThickness="0.1"  
                           IgnoredLayers="{Binding IgnoredLayers}" 
                           RenderTransformOrigin="0.5,0.5"
                           SelectedColor="Black"           
                />
        </DockPanel>
        <Border Background="AliceBlue">
            <Image />
        </Border>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>                
            </Grid.ColumnDefinitions>
            <Grid.Resources>
                <Style TargetType="Border">
                    <Setter Property="BorderBrush" Value="Green"/>                    
                </Style>
            </Grid.Resources>
            <DockPanel Grid.Column="1">                
                <WrapPanel DockPanel.Dock="Bottom">
                    <uc:AxisState CoordinateName="X"/>
                    <uc:AxisState CoordinateName="Y"/>
                    <uc:AxisState CoordinateName="Z"/>
                </WrapPanel>
                <Grid Margin="10">
                    <Border BorderThickness="4,0,0,0"                         
                        Background="{x:Null}">
                        <Border BorderThickness="0,4,0,0" 
                            Background="{x:Null}" >
                            <Border BorderThickness="0,0,4,0"
                                Background="{x:Null}" >
                                <Border BorderThickness="0,0,0,4"                                    
                                    Background="{x:Null}" >
                                    <Canvas>
                                        <Rectangle
                                            Canvas.Bottom="10"
                                            Canvas.Left="20"
                                            Width="60"
                                            Height="48"
                                            Fill="AliceBlue"
                                            RenderTransformOrigin="0.5,0.5"
                                        >
                                            
                                        </Rectangle>
                                    </Canvas>
                                </Border>
                            </Border>
                        </Border>
                    </Border>
                </Grid>
            </DockPanel>
        </Grid>

    </Grid>
</Window>
