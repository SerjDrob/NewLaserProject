<UserControl x:Class="NewLaserProject.Views.Dialogs.CommonDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NewLaserProject.Views.Dialogs"
             mc:Ignorable="d" 
             xmlns:hc="https://handyorg.github.io/handycontrol"
             d:DesignHeight="450" d:DesignWidth="800">
    <Border 
        CornerRadius="10"
        Background="{DynamicResource RegionBrush}">
        <hc:SimplePanel>
            <TextBlock x:Name="Title" VerticalAlignment="Top" Style="{StaticResource TextBlockTitleBold}"/>
            <ContentControl Content="{Binding}" Margin="10,22,10,10"/>
            <Button Width="22" 
                    Height="22" 
                    Command="hc:ControlCommands.Close" 
                    Style="{StaticResource ButtonIcon}" 
                    Foreground="{DynamicResource PrimaryBrush}" 
                    hc:IconElement.Geometry="{StaticResource ErrorGeometry}" 
                    Padding="0" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Top" 
                    Margin="0,4,4,0"/>
        </hc:SimplePanel>
    </Border>
</UserControl>
